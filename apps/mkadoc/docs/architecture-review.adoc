= Architecture review — 2025-09-01

Review of the whole monorepo (core `apps/mkadoc`, `apps/mkadoc-plugin-host`,
`apps/mkadoc-plugin-kroki`, root flake / site config). All 141 tests pass at
review time.

Verdict: solid. The plugin DI container + session-scoped registry and the
incremental-build model are genuinely well-designed. Findings below are mostly
perf, convergence, and small correctness-under-evolution items — nothing
blocking.

== Scope reviewed

* `apps/mkadoc/src` — CLI, config, build/serve orchestration, plugin host +
  loader + registry + installer, incremental-mode decision, dep graph, and the
  five builtin plugins (topbar, nav, shiki, asciidoc, markdown).
* `apps/mkadoc-plugin-host` — contract (JSDoc types) + test host.
* `apps/mkadoc-plugin-kroki` — reference external plugin.
* `apps/mkadoc/flake.nix`, root `flake.nix`, `justfile`, `mkadoc.yaml`.

== Strengths

* **Session discipline.** All cross-build state (registry memoization, nav
  classifier state, plugin disposal bookkeeping, `site-root` redirect) lives in
  one explicit `session` object threaded through `createHosts` — no
  module-global mutable state, easy to reason about under `serve`.
* **Session-scoped dependency registry.** Providers run at most once per
  session while `key` is unchanged (shiki's highlighter builds once per serve
  session); generation-based pruning releases removed providers via
  `onRelease`; a reentrancy guard catches concurrent builds.
* **Security as first-class.** `include::` containment (never reads outside the
  project root), asset-path escape validation, plugin core-module whitelist,
  `ignoreScripts`/`omit:dev` in plugin installs, `.strict()` zod schemas, and
  the deliberate *no HTML rewriting* rule (`chrome-design.adoc` documents why).
* **Deterministic output.** `writeIfChanged`/`copyFileIfChanged` everywhere —
  no mtime churn, stable sorted output.
* **Error ergonomics.** zod-formatted config errors, `mkadoc:`-prefixed user
  errors, load errors listing *every* missing dependency at once.
* **Incremental model.** Mode decision (full/incremental/assets/noop) × dep
  graph × site-wide deps × classifiers × assets-only passes; nav's
  label-staleness classifier is a subtle, well-handled case.
* **Documentation.** `plugins.adoc` is a genuine extension guide with a
  lifecycle timeline; code comments explain *why*.
* **Testing.** 25 files / 141 tests / ~6s, covering external plugins, serve
  smoke tests, and client-JS DOM stubs.

== Findings

Status legend: _open_ — _fixed_ — _won't fix_.

|===
| # | Sev | Area | Finding | Status |

| 1 | P1 | perf | External plugins re-packed on every build / serve rebuild | won't fix |
| 2 | P2 | perf | Source tree walked once per build (page list threaded through) | fixed |
| 3 | P2 | design | Plugin value-sharing converged on DI: service pair + `requires` removed | fixed |
| 4 | P2 | maint | `builtins/nav.js` split into `nav/model.js` + `nav/html.js` + `nav/state.js` | fixed |
| 5 | P3 | correctness | Session discipline complete: `pageMeta` moved into the session; Asciidoctor highlighter is per-load, no global register | fixed |
| 6 | P3 | behavior | `serve` warns when `serve.port`/`serve.remote` config edits need a restart | fixed |
| 7 | P3 | lifecycle | `mkadoc check` disposes plugins | fixed |
| 8 | P3 | behavior | `pluginSignature` canonical — key order ignored | fixed |
| 9 | P3 | behavior | Output pruning handles only `.html`; deleted assets accumulate | won't fix |
| 10 | P3 | robustness | Error classification by `mkadoc:` message prefix | won't fix |
| 11 | P3 | deployment | `mkadoc build` in CI needs network + mutates `.mkadoc/` (external plugin install) | won't fix |
| 12 | P3 | tooling | Hardcoded `npmDepsHash` in `apps/mkadoc/flake.nix` | open |
| 13 | P4 | tooling | No CI configuration in the repo | open |
| 14 | P4 | design | Mounts derived verbatim from source paths (deep URLs) | open |
| 15 | P4 | nits | SSE no heartbeat; installer `.tmp` crash leftovers; `test/**/*.test.js` shell glob | open |
|===

=== 1 (P1) External plugins re-packed on every build — WON'T FIX

`build()` calls `loadPlugins` on *every* build (including each serve rebuild).
For `file:`/`directory` locators, `installLocalPlugin` unconditionally runs
`packPlugin` (`libnpmpack` packs the whole plugin source and writes a tarball
to `.mkadoc/plugins/.tmp`) before the sha512 marker decides whether to
reinstall. The marker avoids reinstall/extract/deps, but **not the pack** — so
every file save under `serve` re-packs the configured external plugin's source
tree.

*Decision (by design):* external local-folder plugins (`file:./path`) are a
plugin-development affordance — always re-packing on load guarantees code
edits are picked up with zero staleness detection to get wrong. The pack cost
only matters under `serve` while actively developing a plugin, where freshness
beats the pack time. No short-circuit added.

=== 2 (P2) Source tree walked 4–5× per build — FIXED

`listSourcePages` (full directory walks) used to run in `decide-mode`
(incremental only), `buildPages`, `deps.retainPages`, `writeSiteIndex`, and
`pruneStaleHtml`; the `buildPages` and `retainPages` inputs were identical.

`build()` now computes the page list **once** per build (after plugins load,
so renderers are registered) and threads it through `decideMode` (new
`allPages` option, with a fallback walk for direct callers), `writeSiteIndex`
and `pruneStaleHtml` (both take the list as a parameter), `buildPages`, and
`deps.retainPages`. `pruneStaleHtml` no longer needs `rendererForPath`.

=== 3 (P2) Three coexisting plugin value-sharing mechanisms — FIXED

The service pair (`provideService`/`getService`) and the legacy
`requires` array on direct-object plugins were **removed entirely**. What
happened:

* `site-root` (nav's choice of where `/` redirects — genuinely build-time,
  derived from current `_nav.adoc` content) is now a **core-provided command
  capability**: the session seeds it via `registry.provideCore('site-root',
  () => (href) => { session.build.siteRoot = href ?? null })`. The injected
  value is a function, not a holder — stable per session (memoized once),
  only its argument changes per build. `mkadoc:nav` declares
  `host.plugin(['site-root'], (setSiteRoot) => …)` and calls it at chrome
  time with its first nav entry's href; `build.js` resets the slot before
  each chrome pass (a plugin removed from config cannot leave a stale
  redirect); `serve`'s dev server reads it per request. Core capabilities
  are a new registry source kind (`core-cap`): never pruned, not shadowable,
  resolvable as DI deps but **not** importable via `host.import`.
* The plugin factory contract now **requires** a `host.plugin(deps, create)`
  declaration — returning a plugin object directly is no longer accepted.
* `provideService`/`getService` removed from `host.js`, the
  `@mkadoc/plugin-host` contract (`contract.js`, `test-host.js`), and
  `plugins.adoc`; the loader's `requires` verification is gone.
* The `session.build.rootRedirect` dead slot (created but never written) is
  replaced by the real `siteRoot` slot.

One model remains: capabilities via `host.provide` (plugins) / `provideCore`
(core) + deps injected into plugin bodies — the runtime service lookup is
gone entirely.

=== 4 (P2) `builtins/nav.js` is a 674-line monolith — FIXED

The nav builtin is split into four files under `src/builtins/nav/`:

* `nav/model.js` (299 LOC) — data model: auto-nav tree (`buildFolder`),
  `_nav.yaml` parsing/validation, page-file lookup, label derivation,
  source-entry resolution.
* `nav/html.js` (215 LOC) — rendering: source-bar, article sidebar, auto-nav
  + yaml HTML, the CSS/JS assets (`NAV_JS` still re-exported from `nav.js`
  for tests).
* `nav/state.js` (72 LOC) — session-scoped classifier state (referenced
  pages + last labels) and the warming pass.
* `nav.js` (138 LOC) — the plugin factory: wires the three into
  `setup`/`contributeChrome`/`check`.

No behavior change; the `mkadoc:nav` locator, options, and assets are
identical.

=== 5 (P3) Session discipline incomplete — FIXED

Both module-global leaks are gone — no mutable module-level state remains:

* `meta-cache.js` is now `createPageMetaCache()` — a cache object owned by the
  **session** (`session.pageMeta`), cleared at the start of each build
  (`build()` calls `session.pageMeta.clear()` instead of a module-level
  `resetPageMetaCache`). Nav (`metaLabelFor(host, …)`) and the site map
  (`host.session.pageMeta.get`) share one parse per page per build; the build
  host gained an `@internal` `session` accessor (mirroring the plugin host),
  and the test host's session stub mirrors the slot. Concurrent builds in one
  process each get their own cache.
* `asciidoc.js` no longer calls the global `SyntaxHighlighter.register`
  (which re-registered a new class per build). Each conversion passes its own
  class through Asciidoctor's per-load `syntax_highlighters` option — a
  per-load override hash — so the global `SyntaxHighlighter` registry is
  never mutated. The class is built per plugin load by
  `serviceSyntaxHighlighter(syntaxHighlight)` and closes over the injected
  `syntax-highlight` capability; the `source-highlighter` attribute is
  unchanged.

=== 6 (P3) `serve` ignores `serve.port`/`serve.remote` config edits — FIXED

`reloadConfigIfNeeded` now compares the reloaded `serve` settings against the
startup binding (`resolveServeListen`) and, when `host`/`port` would change,
logs a warning that a restart is required — the watchers + dev server keep
the old binding. Silent ignore is gone; a restart is the documented way to
rebind.

=== 7 (P3) `mkadoc check` never disposes plugins — FIXED

`check()` wraps the plugin-check loop in `try/finally` and calls
`plugins.dispose()` — the plugin lifecycle is honored on every command, and
a plugin's `check` throwing still releases resources. Covered by a test
(a `file:` plugin writes a marker on dispose; `check` asserts it exists).

=== 8 (P3) `pluginSignature` is `JSON.stringify(cfg.plugins)` — FIXED

`build()` now hashes a canonical form (`pluginSetSignature`): keys sorted
recursively before stringifying, so reordering `plugins:` keys (or an option
object's keys) is the same set and no longer triggers a spurious dispose +
reload under serve. Covered by a test (same set reordered → no dispose).

=== 9 (P3) Output pruning only handles `.html` — WON'T FIX

*Decision (declined):* deleted `_assets/` files, images, and renderer-reported
assets may accumulate in `output/` across incremental builds, but `publish`
(clean) is the deployment path — the stale-asset residue is accepted.

=== 10 (P3) Error classification by message prefix — WON'T FIX

*Decision (declined):* user errors are consistently `mkadoc:`-prefixed by
construction and library errors are surfaced via `err.stack`; the current
heuristic is adequate for the CLI's audience.

=== 11 (P3) Deployment purity — WON'T FIX

*Decision (declined):* external local-folder plugins are a development
affordance (see #1) — build-time install into the gitignored `.mkadoc/` and
the arborist/pacote/libnpmpack runtime footprint are accepted; CI can warm
`.mkadoc/` or use `publish` on a machine that has run `build` once.

=== 12 (P3) Hardcoded `npmDepsHash`

Any npm dependency bump requires manually updating `npmDepsHash` in
`apps/mkadoc/flake.nix`. Use `pkgs.fetchNpmDeps` with a committed derivation or
document the prefetch command.

=== 13–15 (P4) Minor

* No CI workflow despite 141 tests + full nix `checks`.
* Mounts derive verbatim from source paths (this site lives at
  `/apps/mkadoc/docs/...`); a configurable `mount:` option would remove the
  `linkPrefix` special-casing.
* Dev-server SSE has no heartbeat; `installLocalPlugin`'s `.tmp` staging can
  leave strays on crash; `npm test` uses a bash-only `test/**/*.test.js` glob.

== Recommended priority order

. ~~Skip re-packing unchanged external plugins~~ won't fix — by design (1).
. ~~Compute the page list once per build~~ done (2).
. ~~Converge the plugin-authoring model on DI~~ done — service pair + `requires` removed (3).
. ~~Split `nav.js`~~ done (4).
. ~~Session-ize `pageMeta` + constrain the Asciidoctor global~~ done (5).
. ~~Serve: warn on serve.port/serve.remote edits~~ done (6).
. ~~mkadoc check disposes plugins~~ done (7).
. ~~Canonical plugin-set signature (key order ignored)~~ done (8).
. Declined: 9 (pruning), 10 (error prefix), 11 (deployment purity).
. Remaining open: CI (13), mounts (14), nits (15).
